# Fetch the market history for a given region, type_ids
# can accept type_ids from file, or pipe as json list.
#  - should region id be passed in as well?
# can save market histories to a directory.
# can batch fetch market histories to save on memory usage.
# can get all market type_ids if no type_ids are passed in.
# can save as enhanced json, with region_id and type_id in the json.
# can save as jsonl, with region_id and type_id in the json.
