# calculate the market history summary for a given region/type_id
# can accept the market history from file, or pipe, or directory of files.
# can output the summary to a file, or stdout.
# can output the summary in jsonl format, and can append to an existing file.
